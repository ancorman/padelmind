# Mac (Apple Silicon) DEV-ONLY requirements.
# Identical to requirements.txt EXCEPT numpy is relaxed to <2.0 because
# ultralytics 8.3.40 caps numpy<2.0.0 on sys_platform=="darwin".
# Production (RunPod/Linux) uses requirements.txt with numpy==2.2.3 — do NOT
# copy this file into the Docker image.
runpod==1.7.9
ultralytics==8.3.40
opencv-python-headless==4.11.0.86
supervision==0.25.1
numpy==1.26.4
matplotlib==3.10.1
Pillow==11.1.0
boto3==1.37.20
requests==2.32.3
ffmpeg-python==0.2.0
